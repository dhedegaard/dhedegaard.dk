overwrite: true
schema:
  - https://api.github.com/graphql:
      headers:
        Authorization: "Bearer ${GITHUB_PAT}"
documents: "src/**/*.ts"
generates:
  src/codegen/types.ts:
    plugins:
      - "typescript"
